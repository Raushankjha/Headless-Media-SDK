{
  "name": "headless-media-sdk",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "npm run dev --workspace=@headless-media/web",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspace=@headless-media/media-core",
    "lint": "npm run lint --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}
